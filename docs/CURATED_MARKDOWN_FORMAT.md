## All curated chapter files must follow this Markdown structure.

Curated chapter writer follows fixed Markdown format
↓
Loader reads Markdown
↓
Chunker uses heading structure
↓
Retriever finds accurate section
↓
LLM gives grounded answer